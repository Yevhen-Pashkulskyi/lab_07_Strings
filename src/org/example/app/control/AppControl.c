#include <stdio.h>

// char *string(char first_str[], char second_str[], char search, char replace) {
//     char * new_str = malloc()
//
// }
char searchCharacter(char str[], int search_c) {
    for (int i = 0 ; i<) {

    }
    return ;
}

int run() {
    char first_str[] = "Search character";
    char second_str[] = "Replace with a character";
    char search_character, replace_character; //n m
    scanf("%c", &search_character);
    scanf("%c", &replace_character);
    // char *result_str = string(first_str, search_character, search_character, replace_character);

    return 0;
}
